import Register from '/Register';

export const metadata = {
  title: 'Register Account | ErreGeTeYGO',
  description: 'Create an account on ErreGeTeYGO to build custom Yu-Gi-Oh! decks, publish community decklists, and participate in forum discussions.',
};

export default function RegisterPage() {
  return <Register />;
}